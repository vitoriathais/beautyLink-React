import { useNavigate } from 'react-router-dom';
import axios from 'axios';
import { useContext } from 'react';
import { UserContext } from './UserContext';

//Uma tabela que mostre as batidas de ponto de hoje. da tabela controle_ponto.

function config() {
    return (
        <div> 
            <h1>Título</h1>
        </div>
    )
}
//Possibilidade de alterar o horario ou excluir aquela batida de ponto.

export default config()
